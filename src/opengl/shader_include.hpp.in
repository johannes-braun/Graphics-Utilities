#pragma once

#include <filesystem>
#include <vector>

namespace gl::shader_includes
{
    inline std::vector<std::filesystem::path> directories = {"../shaders", "@PROJECT_SOURCE_DIR@/src/shaders"};
}
